Note that these alignment scripts are not necessarily compatible with downstream analyses. Due to the way that DRAGEN handles overlaps and other factors during alignment, it often requires other preprocessing steps and downstream variant callers only present in newer versions of GATK, both of which are not implemented in this repo. Do not attempt to use these scripts as-is without consideration of required changes to downstream analyses.