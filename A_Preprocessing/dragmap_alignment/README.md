Note that this is an placeholder/exploratory script that can be used as reference but is not necessarily complete or compatible with downstream analyses. Due to the way that DRAGEN handles overlaps and other factors during alignment, it requires other preprocessing steps and downstream variant callers only present in newer versions of GATK, which are not implemented in this repo. Do not attempt to use as-is without consideration of required changes to downstream analyses.